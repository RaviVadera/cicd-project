version: '3.9'

services:

  gateway:
    build: gateway/.
    volumes:
      - state_log:/logs
      - docker:/var/run
    ports:
      - 8083:3000

volumes:
  state_log:
  message_log:
    name: message_log
  docker:
    driver: local
    driver_opts:
      type: none
      device: /var/run
      o: bind

networks:
  default:
    name: common_network
